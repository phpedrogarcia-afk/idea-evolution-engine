schema_version: "1.0.0"
description: "Configuração de modelo único para todas as etapas (Baseline / Standard Loop)"
cost_policy: FREE_ONLY
execution_mode: EXPERIMENTAL_PINNED

models:
  primary:
    provider: groq
    model: openai/gpt-oss-120b
    credential_env: GROQ_API_KEY

routes:
  understand: primary
  attack: primary
  critique_1: primary
  revision_1: primary
  critique_2: primary
  revision_2: primary
  alternatives: primary
  reality_check: primary
  synthesize: primary
  final_review: primary

default_model_alias: primary
